import React from "react";
import { View } from "react-native";
import { useAnimationValue } from "../hooks/useAnimationValue";

function StoryExam() {
  const animWidthValue = useAnimationValue(0);

  return <View></View>;
}

export default StoryExam;
